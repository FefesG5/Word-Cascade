import SignOutButton from "@/components/SignOutButton/SignOutButton";

const StudentDashboard = () => {
  return (
    <div>
      <h1>Student Dashboard</h1>
      <SignOutButton />
      {/* Add student-specific components */}
    </div>
  );
};

export default StudentDashboard;
