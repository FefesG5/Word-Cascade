import SignOutButton from "@/components/SignOutButton/SignOutButton";

const TeacherDashboard = () => {
  return (
    <div>
      <h1>Teacher Dashboard</h1>
      <SignOutButton />
      {/* Add teacher-specific components */}
    </div>
  );
};

export default TeacherDashboard;
